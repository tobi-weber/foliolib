{
    "type": "object",
    "description": "Collection of note links",
    "properties": {
        "notes": {
            "type": "array",
            "items": {
                "$ref": "#/noteLinkUpdate"
            }
        }
    },
    "required": [
        "notes"
    ]
}