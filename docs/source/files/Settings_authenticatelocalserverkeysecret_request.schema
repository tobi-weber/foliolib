{
    "description": "Authentication request",
    "type": "object",
    "properties": {
        "key": {
            "description": "key",
            "type": "string",
            "format": "UUID"
        },
        "secret": {
            "description": "secret",
            "type": "string",
            "format": "UUID"
        }
    },
    "additionalProperties": false,
    "required": [
        "key",
        "secret"
    ]
}